0. class: C++에서 객체를 구현하기 위한것.데이터(변수)+기능(함수)를 하나로 묶어놓은 완성형 객체 **설계도**// 실제 존재하는 물건(object)가 아니라,  여기에는 어떤 데이터가 들어가고 어떤 기능이 있어야 해"라고 규칙을 정의해 둔 설계도.


1. 용어:
    1-1  클래스 상에서 변수와 함수 지칭할 때 각각 멤버 변수(member variable) 과 멤버 함수(member function) .


2. class내부의 함수를 설계 하는법 
    2-1: class 내부에서 맴버 함수 전체를 코드.
    //
    2-2: class 내부에서는 매멉 함수 정의만.
    외부에서 함수 전체를 코드.

    >> 간단한 함수를 제외하고 대부분은 2-2방법을 하자.

3. 생성자(Constructor): "객체 생성시 자동으로 호출되는 함수".함수임. 

    3-1형식:
     // 객체를 초기화 하는 역할을 하기 때문에 리턴값이 없음!
     클래스 이름 ( 인자) {}


     e.g.,)
     clss Date{
        ...
        ...
        Date(int year,int month, int day)
        {

        }
     }

     int main()
     {
        Date day(2011, 3, 1);// 암시적 방법. like 함수를 호출하듯이 사용.
        Date day = Date(2012, 3, 1); // 명시적 방법.
     }

     3-2 디폴트 생성자: 
     처음에 생성자를 정의하지 않아도 (디폴트)생성자가 호출됨. 디폴트 생성자는 인자를 하나도 가지지 않는 생성자. 클래스에서 사용자가 어떠한 생성자도 명시적으로 정의하지 않았을 경우에 컴파일러가 자동으로 추가해주는 생성자.// 사용자가 어떤 다른 생성자를 추가한 순간 컴파일러는 자동으로 디폴트 생성자를 삽입하지 않는 다는 것을 명심.
    
    e.g.,)//사용자가 직접 디폴트 생성자를 정의한 경우.

    class Date
    {
        private:
        int year,month,day;
        ...
        ...
        Date()
        {
            year=2017;
            month=7;
            day=12;
        }

    }

    int mian()
    {
        Date day1=Date(); //디폴트 생성자에 의해 day1의 year 에는 2012, month 에는 7, day 에는 12
        Date day2;  //디폴트 생성자에 의해 day2의 year 에는 2012, month 에는 7, day 에는 12
        Date day3();// 안됨.디폴트 생성자를 이용해서 초기화 하는 것이 아니라, 리턴값이 Date 이고 인자가 없는 함수 day3 을 정의하게 된 것으로 인식.

    }

    3-3 고민: 컴파일러의 디폴트 생성자를 사용하고 싶을땐 그냥 생성자를 정의하지 않는 방법 밖에 없었음. so 코드를 읽는 사람 입장에선 임마가 생성자를 잊어먹은건지 or 컴파일러의 디폴트 생성자를 use 하고픈지 알길이 없었음.
    >> 해결방법: 생성자의 선언 바로 뒤에 = default 붙이기.(이렇게 하면 클래스의 디폴트 생성자를 정의하라고 컴파일러에게 명시적으로 알려줄수 있음.)

    e.g.,)
    class Test {
    public:
    Test() = default;  // 디폴트 생성자를 정의해라
    };


4. 생성자-함수 오버로딩: 
생성자도 함수. so 함수 오버로딩 ㄱㄴ. >> 해당 클래스의 객체를 여러가지 방식으로 생성할 수 있게됨.