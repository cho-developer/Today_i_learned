0. 힙// 임마 쓰는 이유: 루트에 최대/최소 값을 두어서 값   빨리 찾으려고. so 힙 자료구조에서 유의미한 값은 루트의 값.
  0-1 힙 용어
   Complete Binary Tree로 구성.
   MAX_HEAP: 루트에 최대값.// 부모 노드 값이 무조건 자식들보다 크거가 같음. (자식들 순서는 상관X) 
   MIN_HEAP: 루트에 최솟값.// 부모 노드 값이 무조건 자식들보다 작거나 같음. (자식들 순서는 상관X)
   Heapify: 어떤 자료구조를 MAX_Heap or MIN_heap으로 바꾸는 작업. 자식 노드가 있는 노드들만 shiftdown.( 자식 노드둘을 비교. 둘중 크or 작은걸 부모랑 비교. 교체 필요하면 교체. 교체하고 나서  안맞을수 있으니 다시 비교해야함).
   원소 추가: 무조건 마지막 레벨의 왼쪽 끝에 추가. 또 힙구조에 안맞을수 있으니 비교.//자식 노드 입장에서 부모노드 와 비교. 부모노드 인덱스 = (자식 노드 인덱스 -1)/2
   heapfity, 즉 힙구조로의 전환을 하는 이유: 최대/최소를 찾는게 O(logN)만큼 만 걸림. 즉, 데이터 전체를 비교하지 않고 최대/최소값을 구할수 있음. (다른건 O(N) 걸림.)-->>>>Q:어케 데이터 전체를 비교하지 않고 최대/최소를 찾을수 있냐????? A:데이터가 그냥 아무렇게나 흩뿔려지지 않고 complete binary tree로 이뤄져있다고 가정해서.(complete binary tree: 부모 노드수는 전체 노드수/2 이니깐! 절반만 비교하면 끝.)

   heap 구조에서 노드를 추가or 삭제: 
   추가 할땐 shift up, 
   삭제할땐 맨위 루트에다가 맨 마지막 노드의 값을 대입하고 shift down.O(log(N)).




   원소 삭제: 힙에서 삭제는 무조건 레벨 0루트만.(가장 밑에 있는 값을 루트에 대입. 가장 밑에 있는 노드 삭제. 또 힙 구조에 안맞을수 있으니 비교.) 
  0-2 부모-자식 인덱스 관계 
    int childidx1 = parentidx * 2+1;
    int childidx2 = (parentidx +1 )*2;
  0-3 자식 있는 노드-전체 노드수의 관계
    자식이 있는노드(부모 느드)수 =전체 노드수/2;
    

1. 힙 구조체

e.g.,)

typedef struct heap
{
  int *arr; //heap 데이터가 저장될곳.
  int size;// 저장된 원소(데이터) 개수
  int cap;//  최대 저장 가능 원소(데이터) 개수.
}heap;




