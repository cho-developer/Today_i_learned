0. 재귀함수.//반복문으로 풀수 있는 문제는 반복문으로 해라.
    0-1재귀함수: 자기를 다시 호출 하는함수.
    0-2재귀함수 로직: 하나의 재귀함수 하나에서 해결하지말고 호출할때마다 실제 메모리 상에서 잡히는걸  그리기(생각). 
    //함수는 호출되는 순간마다 메모리에 잡힘.

1. return: return은 호출한곳으로 돌아가고 return하는 함수는 메모리 상에서 사라짐(함수 끝남). 
    1-1++:중간에 reutrn을 만나지 않고 함수가 끝까지 돎: 호출된 곳으로 돌아가고 끝까지 돈 함수는 메모리 상에서 사라짐(함수 끝남).
    //so. 재귀호출하는 코드 위치에 따라 완전히 달라짐.

3. return 에 재귀호출하는 함수가 2개 있을때 로직: 앞의 함수를 다 수행하고 뒤의 함수로 move. 


int square(int mit, int jisu);

int main(){

    square(2,4);

}
int square(int mit, int jisu){

        if(jisu==0)return 1;


        return mit*square(mit,jisu-1);
}

void EUC_Alg (int a, int b){
    
    if(b==0){ return a;}

   return EUC_Alg(b, a%b);
}




int fibonacci(int a, int b, int cnt){

    if(cnt==1){
        return a+b;
    }
    else{
        fibonacci(b, a+b,cnt-1);
    }

}